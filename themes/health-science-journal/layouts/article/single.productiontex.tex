%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% {{ .Title }}
% Article frontmatter generated by Hugo

%\documentclass[letterpaper]{ehi-journals}
\documentclass[a4paper,num-refs]{ehi-journals}

%%% Journal toggle; only specific options recognised.
%%% (Only "neurobehav" is implemented now.)
\journal{neurobehav}

%%% Watermark toggles
%%% Accepts "draft", "proof" or "retracted". Setting any other value will remove the watermark.
\watermark{proof}

\usepackage{graphicx}
\usepackage{siunitx}
\usepackage{academicons}
\usepackage{hyperxmp}
\definecolor{orcidlogocol}{HTML}{A6CE39}
\newcommand{\orcid}[1]{\href{https://orcid.org/#1}{\textcolor[HTML]{A6CE39}{\aiOrcid}}}

%%% PDF metadata
%%% This generates the metadata for the PDF

\title{{ "{" }}{{ .Title }}{{ "}" }}
{{ $eqfootnote := 1 }}{{ $corfootnote := 1 }}{{ $decfootnote := 1 }}{{ $setdec := 0 }}{{ $seteq := 0 }}{{ range .Page.Params.authors }}{{ if in .corresp "@" }}{{ $eqfootnote = add $eqfootnote 1 }}{{ end }}{{ end }}{{ $decfootnote = add $eqfootnote 1 }}

{{ range .Page.Params.authors }}
\author[{{ $affils := len .affiliations }}{{ range $elem_index, $elem_val := .affiliations }}{{ . }}{{ if lt $elem_index (sub $affils 1) }}, {{ end }}{{ end }}{{ if in .equalcontrib "yes" }},\authfn{{ "{" }}{{ $eqfootnote }}{{ "}" }}{{ $seteq = 1 }}{{ end }}{{ if in .corresp "@" }},\authfn{{ "{" }}{{ $corfootnote }}{{ "}" }}{{ $corfootnote = add $corfootnote 1 }}{{ end }}{{ if in .deceased "yes" }},\authfn{{ "{" }}{{ $decfootnote }}{{ "}" }}{{ $setdec = 1 }}{{ end }}]{{ "{" }}{{ .given_name }} {{ .surname }}{{ with .ORCID }}\orcid{{ "{" }}{{ . }}{{ "}" }}{{ end }}{{ "}" }}{{ end }}]

{{ range $elem_index, $elem_var := .Page.Params.affiliations }}
\affil[{{ add $elem_index 1 }}]{{ "{" }}{{ .name }}{{ "}" }}{{ end }}

{{ $corfootnote = 1 }}{{ range .Page.Params.authors }}{{ if in .corresp "@" }}
\authnote{\authfn{{ "{" }}{{ $corfootnote }}{{ "}" }}{{ .corresp }}{{ "}" }}{{ $corfootnote = add $corfootnote 1 }}{{ end }}{{ end }}
{{ if eq $seteq 1 }}\authnote{\authfn{{ "{" }}{{ $eqfootnote }}{{ "}" }}Authors contributed equally}{{ end }}
{{ if eq $setdec 1 }}\authnote{\authfn{{ "{" }}{{ $decfootnote }}{{ "}" }}Deceased}{{ end }}

\papercat{{ "{" }}{{ if in ( lower .Page.Params.articleType ) "editorial"  }}Editorial{{ else if in ( lower .Page.Params.articleType ) "perspective" }}Professional Perspectives{{ else if in ( lower .Page.Params.articleType ) "brief" }}Brief Communication{{ else if in ( lower .Page.Params.articleType ) "research" }}Research Article{{ else if in ( lower .Page.Params.articleType ) "registered" }}Registered Report{{ else if in ( lower .Page.Params.articleType ) "review" }}Review{{ else if in ( lower .Page.Params.articleType ) "commentary" }}Commentary{{ else if ( lower .Page.Params.articleType ) "correspondence" }}Correspondence{{ else if in ( lower .Page.Params.articleType ) "data" }}Data Brief{{ else if in ( lower .Page.Params.articleType ) "correction" }}Correction{{ else if in ( lower .Page.Params.articleType ) "concern" }}Expression of Concern{{ else if in ( lower .Page.Params.articleType ) "retraction" }}Retraction{{ else }}{{ .Page.Params.articleType }}{{ end }}{{ "}" }}
\runningauthor{{ "{" }}{{ if gt ( len .Page.Params.authors ) 2 }}{{ range first 1 .Page.Params.authors }}{{ .surname }} et al.{{ end }}{{ else if gt ( len .Page.Params.authors ) 1 }}{{ range $elem_index, $elem_val := .Page.Params.authors }}{{ .surname }}{{ if eq $elem_index 0 }} and {{ end }}{{ end }}{{ else }}{{ range first 1 .Page.Params.authors }}{{ .surname }}{{ end }}{{ end }}{{ "}" }}

%%% Keywords list
\newcommand{\kwdone}{{ "{" }}{{ index .Page.Params.keywords 0 }}{{ "}" }}
\newcommand{\kwdtwo}{{ "{" }}{{ index .Page.Params.keywords 1 }}{{ "}" }}
\newcommand{\kwdthree}{{ "{" }}{{ index .Page.Params.keywords 2 }}{{ "}" }}
\newcommand{\kwdfour}{{ "{" }}{{ index .Page.Params.keywords 3 }}{{ "}" }}
\newcommand{\kwdfive}{{ "{" }}{{ index .Page.Params.keywords 4 }}{{ "}" }}
\newcommand{\kwdsix}{{ "{" }}{{ index .Page.Params.keywords 5 }}{{ "}" }}

%%% Article metadata automatically set for the current year. Uncomment to set values if required.
\jyear{{ "{" }}{{ dateFormat "2006" .Date }}{{ "}" }}
{{ with .Page.Params.issue }}\jnumber{{ "{" }}{{ . }}{{ "}" }}{{ end }}
{{ with .Page.Params.volume }}\jvolume{{ "{" }}{{ . }}{{ "}" }}{{ end }}

%%% Article metadata that needs to be set for each article.
\jarticlenum{{ "{" }}{{ .Page.Params.firstpage }}{{ "}" }}
\crossmarkdate{{ "{" }}{{ dateFormat "2006-01-02" .Date }}{{ "}" }}

\jdoi{10.35430/nab.\setjyear.\setjarticlenum}

\newtoggle{NC}{{ if in .Page.Params.license "creativecommons.org/licenses/by/4.0" }}
\togglefalse{NC} %%% Uses the default CC BY {{ else if in .Page.Params.license "creativecommons.org/licenses/by-nc/4.0" }}
\toggletrue{NC} %%% Uses the CC BY-NC licenses which restricts commercial reuse of the article{{ end }}

\begin{document}

\begin{frontmatter}
\maketitle
\begin{abstract}
{{ .Page.Params.abstract }}
\end{abstract}

\begin{keywords}
\kwdone; \kwdtwo; \kwdthree; \kwdfour; \kwdfive; \kwdsix
\end{keywords}
\end{frontmatter}

\section{Introduction}

%%% Typeset article content here %%%

